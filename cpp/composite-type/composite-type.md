## 复合类型

这种类型是基于基本整型和浮点类型创建的  


### 数组

array
: 数组是一种数据格式，能够存储多个同类型的值  


数组声明应指出以下三点：
* 存储在每个元素中的值的类型
* 数组名
* 数组中的元素数


声明数组的通用格式：  
```txt
typeName arrayName[arraySize];
```

> 表达式 arrayName 指定元素数目，它必须是 **整型常量** 或 **const 值**，也可以是 **常量表达式**  
>
